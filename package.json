{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../../dev && npm run build && cp -r dist ../part3/test && cd ../part3/test",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "node --test",
    "lint": "eslint ."
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "mongoose": "^8.2.3"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin-js": "^1.7.0",
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0"
  }
}
